let initialState = {
    friends: [
      { id: 1, name: "Kolya" },
      { id: 2, name: "Yana" },
      { id: 3, name: "Evgen" },
    ],
  };

const sidebarReducer = (state = initialState, action) => {

    return state;
}

export default sidebarReducer;